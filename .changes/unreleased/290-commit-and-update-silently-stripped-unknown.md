<!-- okffs:type=Changed -->
- commit_and_update: silently stripped unknown params let `commit_message` vanish — rename `hint` to `message` and reject… ([#290](https://github.com/neturely/okffs/issues/290))
